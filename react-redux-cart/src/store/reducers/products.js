const initialState = [
  {
    id: "1",
    name: "iphone 12",
    price: 9999,
    inventory: 100
  }
]
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
